---
title: "A Journal of my Experiments"
description: "A kitchen-sink post that exercises every MDX feature the blog supports — headings, inline styles, links, lists, tables, blockquotes, images, and the full range of code-block variants — so the styling can be reviewed end-to-end on one page."
date: "2025-12-09"
category: "Log"
icon: "QuestionMarkIcon"
heroImage: "https://picsum.photos/1200/600"
authors:
    - name: "Karan"
      url: "https://x.com/naraklog"
---

This post is a visual audit of every MDX feature the blog currently supports. Each section exercises one primitive so styling issues are easy to spot. Normal paragraphs look like this one: a regular block of prose with generous line-height, subtle letter-spacing, and `space-y-4` separating it from neighbours.

A second paragraph immediately after, to verify vertical rhythm between consecutive `<p>` elements.

## Headings

Every heading level below is wrapped in the `LinkedHeading` component, so hovering reveals an anchor-link icon that copies the URL on click.

# This is an h1

## This is an h2

### This is an h3

#### This is an h4

##### This is an h5

###### This is an h6

## Inline text styles

You'll see **bold text** used for emphasis, _italic text_ for softer emphasis or titles, and `inline code` for variable names, file paths, or short snippets. You can combine them: **`bold monospace`** for something that's both important and code-like, or **_bold italic_** when two kinds of emphasis stack.

Occasionally there's a reason to ~~strike something through~~ — usually when I've changed my mind and want to leave the original visible for context.

A paragraph with an inline [internal link to the blog index](/blog), an [external link to 63bytes.com](https://63bytes.com), and a bare autolink: https://example.com. External links should open in a new tab; internal links should use client-side navigation.

## Blockquotes

> This is a blockquote. It can span a single line or wrap across several, and it's useful when I want to set someone else's words — or a past version of my own — apart from the flow of the post.
>
> A second paragraph inside the same blockquote, to verify how stacked quote blocks render.

> A second, standalone blockquote containing **bold**, _italic_, `inline code`, and a [link](https://63bytes.com) so the inline styling inside quotes can be checked in one place.

## Horizontal rules

The next line is a horizontal rule:

---

And the content continues after it.

## Unordered lists

- The question I'm starting from
- Constraints I'm working within
- Things I'm deliberately not solving yet
- Open questions I expect to revisit — with **bold**, _italic_, and `code` inline to check list-item styling

## Ordered lists

1. Understand the problem before touching the implementation
2. Design the data model first
3. Write the happy path
4. Handle the edges
5. Delete what you don't need

## Nested lists

- System design
  - Data model
  - API shape
    - Request schema
    - Response schema
  - State ownership
- Implementation
  - Backend first
  - Frontend against a stub
  - Integration last

## Task lists

- [x] Draft the feature-audit post
- [x] Include every primitive at least once
- [ ] Verify styling in dark mode
- [ ] Verify styling in light mode
- [ ] File follow-ups for anything that looks off

## Tables

| Feature          | Source             | Status   |
| ---------------- | ------------------ | -------- |
| Headings         | `LinkedHeading`    | Styled   |
| Links            | `MdxLink`          | Styled   |
| Code blocks      | `CodeBlock`        | Styled   |
| Tables           | GFM + custom CSS   | Styled   |
| Task lists       | GFM                | Default  |
| Blockquotes      | Markdown           | Default  |
| Images           | Markdown           | Default  |

A second table, wider, to test horizontal overflow on small screens:

| Language   | Extension | Runtime         | Package manager | Notable users                      |
| ---------- | --------- | --------------- | --------------- | ---------------------------------- |
| TypeScript | `.ts`     | Node / Bun / Deno | `npm` / `pnpm` / `bun` | Most of the modern web frontend stack |
| Python     | `.py`     | CPython / PyPy  | `pip` / `uv`    | Data, ML, scripting, backend services |
| Rust       | `.rs`     | Native          | `cargo`         | Systems, CLIs, performance-critical code |
| Go         | `.go`     | Native          | `go mod`        | Backends, networking, infrastructure |
| Zig        | `.zig`    | Native          | `zig build`     | Low-level systems, embedded, compilers |

## Images

A Markdown image with a visible caption beneath it:

<figure>

![A placeholder image from picsum, 1200x600](https://picsum.photos/1200/600)

<figcaption>A moment captured in the frame — sample image courtesy of Lorem Picsum.</figcaption>
</figure>

And one without a caption, which still gets its own figure number:

![Another placeholder image from picsum, 800x500](https://picsum.photos/800/500)

## Code blocks

### Plain fenced block

```
A fenced block with no language. Useful for plain text, logs,
or quick snippets that don't need syntax highlighting.
```

### TypeScript

```typescript
type DebounceOptions = {
  leading?: boolean;
  trailing?: boolean;
};

function debounce<T extends (...args: unknown[]) => void>(
  fn: T,
  wait: number,
  options: DebounceOptions = {}
): (...args: Parameters<T>) => void {
  const { leading = false, trailing = true } = options;
  let timer: ReturnType<typeof setTimeout> | null = null;
  let lastArgs: Parameters<T> | null = null;

  return function (...args: Parameters<T>) {
    lastArgs = args;
    if (leading && timer === null) fn(...args);
    if (timer !== null) clearTimeout(timer);
    timer = setTimeout(() => {
      timer = null;
      if (trailing && lastArgs !== null) fn(...lastArgs);
    }, wait);
  };
}
```

### JavaScript

```javascript
const sum = (a, b) => a + b;
const nums = [1, 2, 3, 4, 5];
const total = nums.reduce(sum, 0);
console.log(total);
```

### Python

```python
from functools import wraps
from typing import Callable, TypeVar, Any

F = TypeVar("F", bound=Callable[..., Any])


def memoize(fn: F) -> F:
    cache: dict = {}

    @wraps(fn)
    def wrapper(*args: Any) -> Any:
        if args not in cache:
            cache[args] = fn(*args)
        return cache[args]

    return wrapper  # type: ignore[return-value]
```

### Rust

```rust
fn fib(n: u64) -> u64 {
    match n {
        0 => 0,
        1 => 1,
        _ => fib(n - 1) + fib(n - 2),
    }
}

fn main() {
    for i in 0..10 {
        println!("fib({}) = {}", i, fib(i));
    }
}
```

### Go

```go
package main

import "fmt"

func main() {
	nums := []int{1, 2, 3, 4, 5}
	total := 0
	for _, n := range nums {
		total += n
	}
	fmt.Println("total:", total)
}
```

### Bash

```bash
#!/usr/bin/env bash
set -euo pipefail

INTERVAL="${1:-10}"
LOG_FILE="${2:-/tmp/watch.log}"

echo "Watching every ${INTERVAL}s, logging to ${LOG_FILE}"

while true; do
  echo "[$(date -u +%Y-%m-%dT%H:%M:%SZ)] tick" >> "$LOG_FILE"
  sleep "$INTERVAL"
done
```

### CSS

```css
:root {
  --brand: oklch(0.7 0.2 280);
  --radius: 0.625rem;
}

.card {
  border: 1px solid var(--brand);
  border-radius: var(--radius);
  padding: 1rem 1.25rem;
}
```

### HTML

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Hello</title>
  </head>
  <body>
    <main>
      <h1>Hello, world</h1>
    </main>
  </body>
</html>
```

### JSON

```json
{
  "name": "63bytes",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run dev",
    "build": "bun run build",
    "start": "bun run start"
  }
}
```

### YAML

```yaml
name: ci
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bun install
      - run: bun run build
```

### Zig

```zig
const std = @import("std");

pub fn main() !void {
    const stdout = std.io.getStdOut().writer();
    try stdout.print("Hello, {s}!\n", .{"world"});
}
```

### Titled code block

```typescript title="utils/debounce.ts"
export function debounce<T extends (...args: unknown[]) => void>(
  fn: T,
  wait: number
): (...args: Parameters<T>) => void {
  let timer: ReturnType<typeof setTimeout> | null = null;
  return (...args) => {
    if (timer) clearTimeout(timer);
    timer = setTimeout(() => fn(...args), wait);
  };
}
```

### Titled code block with caption

```python title="scripts/watch.py" caption="A minimal file-watcher loop, useful for smoke-testing pipelines."
import time

while True:
    print("tick")
    time.sleep(1)
```

### Line numbers

```typescript showLineNumbers
function greet(name: string) {
  const greeting = `Hello, ${name}!`;
  console.log(greeting);
  return greeting;
}

greet("world");
greet("63bytes");
```

### Line highlighting

```typescript {2,5-7}
function getTickStep(): number {
  const scrollable = document.documentElement.scrollHeight - window.innerHeight;
  if (scrollable > 12000) return 1;
  if (scrollable > 6000)  return 2;
  if (scrollable > 3000)  return 5;
  if (scrollable > 1500)  return 8;
  return 10;
}
```

### Everything at once: title, caption, line numbers, highlighted lines

```typescript title="hooks/useTickStep.ts" showLineNumbers {4-6} caption="Derives a scroll-tick step from total scrollable height so long posts don't feel silent."
import { useEffect, useState } from "react";

export function useTickStep() {
  const [step, setStep] = useState(10);

  useEffect(() => {
    const compute = () => {
      const scrollable =
        document.documentElement.scrollHeight - window.innerHeight;
      if (scrollable > 12000) setStep(1);
      else if (scrollable > 6000) setStep(2);
      else if (scrollable > 3000) setStep(5);
      else setStep(10);
    };
    compute();
    window.addEventListener("resize", compute);
    return () => window.removeEventListener("resize", compute);
  }, []);

  return step;
}
```

## Language title icon audit

Every language in `public/icons/langs/` rendered as a titled code block, so the injected monochrome icon can be spot-checked against the title text colour.

```typescript title="typescript.ts"
const greet = (name: string): string => `Hello, ${name}!`;
```

```javascript title="javascript.js"
const greet = (name) => `Hello, ${name}!`;
```

```python title="python.py"
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

```rust title="rust.rs"
fn greet(name: &str) -> String {
    format!("Hello, {}!", name)
}
```

```go title="go.go"
package main

import "fmt"

func greet(name string) string {
	return fmt.Sprintf("Hello, %s!", name)
}
```

```bash title="bash.sh"
greet() { echo "Hello, $1!"; }
greet "world"
```

```css title="css.css"
.greet::before {
  content: "Hello, ";
}
```

```html title="html.html"
<p>Hello, <strong>world</strong>!</p>
```

```json title="json.json"
{
  "greet": "Hello, world!"
}
```

```yaml title="yaml.yaml"
greet: "Hello, world!"
```

```zig title="zig.zig"
const std = @import("std");

pub fn main() !void {
    try std.io.getStdOut().writer().print("Hello, {s}!\n", .{"world"});
}
```

```plaintext title="unknown.txt"
A language with no icon mapping should fall back to the generic code glyph.
```

## Mixing primitives

One last paragraph that mixes **bold**, _italic_, ~~strikethrough~~, `inline code`, and a [link](/blog) together, immediately followed by a list:

1. A numbered step with a `code identifier` inside.
2. A step with a [link to an internal route](/about).
3. A step with an [external link](https://example.com) and **bold** emphasis.

And a final blockquote to close things out:

> If something on this page looks off — spacing, colour, alignment, overflow on small screens, dark vs light mode — it's a styling bug worth fixing. That's the whole point of this post.
